/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: [
        "utfs.io",
        "pixner.net",
        "res.cloudinary.com"
      ]
    }
  }
  
  module.exports = nextConfig
